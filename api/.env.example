# ---- MongoDB ----------------------------------------------------------------
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=abhishek

# ---- Cloudinary (Dashboard > Product Environment Credentials) ---------------
CLOUDINARY_CLOUD_NAME=ttxogdlp
CLOUDINARY_API_KEY=223381845214137
CLOUDINARY_API_SECRET=grYNCm9PP1G8KoOhqlkmMD21ptU

# ---- Cloudflare Pages deploy hook ------------------------------------------
# Pages project > Settings > Builds & deployments > Deploy hooks > Create.
DEPLOY_HOOK_URL=

# ---- Access control ---------------------------------------------------------
# Comma-separated origins allowed to call this API. Leave empty to allow all
# (fine while testing, tighten before you point a real domain at it).
ALLOWED_ORIGINS=https://your-site.pages.dev,http://localhost:4321

# Leave EMPTY for no authentication (the setup you asked for).
# Set any long random string to require it — see README "Locking down the admin".
ADMIN_TOKEN=
