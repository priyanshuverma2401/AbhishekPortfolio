# Read at BUILD time only. Set these in Cloudflare Pages > Settings > Variables.
# The public site never talks to MongoDB at runtime.
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=abhishek
